const cursor=document.getElementById('cursor'),trailContainer=document.getElementById('trail-container');let mouseX=0,mouseY=0;document.addEventListener('mousemove',(e)=>{mouseX=e.clientX;mouseY=e.clientY;cursor.style.left=mouseX+'px';cursor.style.top=mouseY+'px';if(Math.random()>0.3){const p=document.createElement('div');p.className='trail-particle';const s=Math.random()*10+3;const h=Math.random()>0.5?180:300;p.style.cssText=`left:${mouseX}px;top:${mouseY}px;width:${s}px;height:${s}px;background:hsla(${h},100%,60%,${Math.random()*0.5+0.3});box-shadow:0 0 ${s}px hsla(${h},100%,60%,0.8);`;trailContainer.appendChild(p);setTimeout(()=>p.remove(),800)}});document.querySelectorAll('a,button,.portfolio-item').forEach(el=>{el.addEventListener('mouseenter',()=>cursor.classList.add('hover'));el.addEventListener('mouseleave',()=>cursor.classList.remove('hover'))});const musicBtn=document.getElementById('musicBtn'),bgMusic=document.getElementById('bgMusic');let isPlaying=false;musicBtn.addEventListener('click',()=>{if(isPlaying){bgMusic.pause();musicBtn.classList.remove('playing')}else{bgMusic.play().catch(e=>alert('无法播放音频'));musicBtn.classList.add('playing')}isPlaying=!isPlaying});document.body.addEventListener('click',()=>{if(!isPlaying){bgMusic.play().then(()=>{isPlaying=true;musicBtn.classList.add('playing')}).catch(()=>{})}},{once:true});document.querySelectorAll('.portfolio-item video').forEach(v=>{v.parentElement.addEventListener('mouseenter',()=>v.play());v.parentElement.addEventListener('mouseleave',()=>v.pause())});window.addEventListener('load',()=>{setTimeout(()=>{document.getElementById('loading').classList.add('hidden')},1500)});document.querySelectorAll('nav a').forEach(l=>{l.addEventListener('click',(e)=>{e.preventDefault();const t=document.querySelector(l.getAttribute('href'));t.scrollIntoView({behavior:'smooth',block:'start'})})});
